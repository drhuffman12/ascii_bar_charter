name: Crystal CI

on:
  push:
    branches: [ master ]
    # branches: [ master, drhuffman12/* ]
  pull_request:
    branches: [ master ]
    # branches: [ master, drhuffman12/* ]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: crystallang/crystal

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: shards install
    - name: Check color enabled
      run: NO_COLOR=0 echo $NO_COLOR
    - name: Run tests
      run: NO_COLOR=0 crystal spec
      # run: bin/test
